"""ascir_common — shared utilities used across ASCIR tooling.

Currently holds the canonical manifest-hash implementation imported by
both the dataset generator and the detector, so the two can never drift.
"""
